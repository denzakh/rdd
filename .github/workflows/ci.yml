name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      # actions ≥ v5 объявляют рантайм node24 (иначе GitHub ругается на Node 20)
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: FSD layers (steiger)
        run: npm run steiger

      - name: Unit tests
        run: npx vitest run

      - name: Integration tests (local D1)
        run: npm run test:db
